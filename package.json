{
  "name": "@zimtsui/binary-heap",
  "version": "0.0.2",
  "description": "",
  "repository": {
    "type": "git",
    "url": "https://github.com/zimtsui/binary-heap.git"
  },
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "scripts": {
    "test": "tsc -b ./test/src/tsconfig.json && ava",
    "build": "tsc -b ./src/tsconfig.json",
    "clean": "rm -rf ./build",
    "docs": "typedoc --tsconfig ./src/tsconfig.json --excludePrivate --excludeProtected --cleanOutputDir ./src/index.ts"
  },
  "author": "Zim",
  "dependencies": {
    "@types/assert": "^1.5.6",
    "assert": "^1.5.0"
  },
  "devDependencies": {
    "ava": "^4.2.0"
  }
}
